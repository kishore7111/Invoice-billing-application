---
description: Start the development server
---

To start the invoice billing application development server, follow these steps:

1. Ensure you are in the project root directory (`d:\Invoice Billing application`).
// turbo
2. Run the development server command:
```bash
npm run dev
```
3. Open your browser to the URL displayed in the terminal (usually `http://localhost:5173`).
